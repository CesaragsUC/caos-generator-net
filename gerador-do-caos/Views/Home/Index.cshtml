<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Stress Test Application</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <style>
        body {
            font-family: Arial, sans-serif;
            transition: background-color 0.5s ease;
            background-image: url('https://pbs.twimg.com/media/Eyd_T8WU4Agz_Um.png'); /* Link para uma imagem de fundo que represente o caos */
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            background-attachment: fixed;
            color: white;
        }

        .overlay {
            background-color: rgba(0, 0, 0, 0.6); /* Sobreposição escura para melhorar a legibilidade */
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
        }

        .container {
            margin-top: 50px;
            position: relative;
            z-index: 1;
        }

        .card {
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            margin-bottom: 20px;
            background-color: rgba(255, 255, 255, 0.9); /* Fundo semi-transparente para os cards */
        }

        .btn-custom {
            font-size: 1.2rem;
            width: 100%;
            padding: 15px;
            margin-top: 10px;
            transition: background-color 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
        }

            .btn-custom i {
                margin-right: 10px;
            }

            .btn-custom:hover {
                background-color: #495057;
            }

        .btn-high-cpu {
            background-color: #dc3545;
            color: white;
        }

        .btn-high-memory {
            background-color: #ffc107;
            color: white;
        }

        .btn-crash {
            background-color: #343a40;
            color: white;
        }

        .btn-unavailable {
            background-color: #6c757d;
            color: white;
        }

        .card-header {
            background-color: transparent;
            color: white;
            border-radius: 10px 10px 0 0;
            padding: 15px;
            text-align: center;
        }

        .kubernetes-logo {
            width: 50px;
            height: 50px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="card">
            <div class="card-header">
                <img src="https://upload.wikimedia.org/wikipedia/commons/3/39/Kubernetes_logo_without_workmark.svg" style="width:auto;height:100px; "
                     alt="Kubernetes Logo" class="kubernetes-logo">
            </div>
            <div class="card-body">
                <p class="card-text">Use the buttons below to simulate different stress scenarios in your application.</p>
                <form asp-action="HighCpuUsage" method="post">
                    <button type="submit" class="btn btn-custom btn-high-cpu" onclick="changeBackgroundColor('#dc3545')">
                        <i class="fas fa-microchip"></i> High CPU Usage
                    </button>
                </form>
                <form asp-action="HighMemoryUsage" method="post">
                    <button type="submit" class="btn btn-custom btn-high-memory" onclick="changeBackgroundColor('#ffc107')">
                        <i class="fas fa-memory"></i> High Memory Usage
                    </button>
                </form>
                <form asp-action="CrashApplication" method="post">
                    <button type="submit" class="btn btn-custom btn-crash" onclick="changeBackgroundColor('#343a40')">
                        <i class="fas fa-bomb"></i> Crash Application
                    </button>
                </form>
                <form asp-action="TemporaryUnavailability" method="post">
                    <button type="submit" class="btn btn-custom btn-unavailable" onclick="changeBackgroundColor('#6c757d')">
                        <i class="fas fa-hourglass-half"></i> Temporary Unavailability
                    </button>
                </form>
            </div>
        </div>
    </div>

    <script>
        function changeBackgroundColor(color) {
            document.body.style.backgroundColor = color;
        }
    </script>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
